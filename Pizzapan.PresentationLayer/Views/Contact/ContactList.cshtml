@model List<Contact>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

<h1>Gelen Mesajlar</h1>

<table class="table table-bordered">
    <tr style="background-color:brown; color:white">
        <th>#</th>
        <th>Gönderen</th>
        <th>Konu</th>
        <th>Tarih</th>
        <th>Mesajı Sil</th>
        <th>Mesajı Gör</th>
    </tr>
    @foreach (var item in Model)
    {
        count++;
        <tr>
            <td>@count</td>
            <td>@item.Name</td>
            <td>@item.Subject</td>
            <td>@Convert.ToDateTime(@item.SendDateMessage).ToString("dd/MM/yyyy")</td>
            <td> <a href="/Contact/DeleteContact/@item.ContactId" class="btn btn-danger">Sil</a>  </td>
            <td> <a href="/Contact/MessageDetailsContact/@item.ContactId" class="btn btn-info">Detay</a> </td>
        </tr>
    }
</table>